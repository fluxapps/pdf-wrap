{
  "name": "simple-viewer",
  "version": "1.0.0",
  "description": "Basic example of PDF Wrap",
  "main": "index.js",
  "author": "studer + raimann ag",
  "license": "GPL-3.0",
  "scripts": {
    "copyPDFjs": "mkdir -p build/www/assets/libs && cp -r node_modules/pdf-wrap/node_modules/pdfjs-dist build/www/assets/libs",
    "copyResources": "cp src/index.html build/www && cp -r src/assets/* build/www/assets",
    "build": "yarn webpack && yarn run copyResources && yarn run copyPDFjs",
    "clean": "rm -r build",
    "start": "yarn run build && yarn light-server -s build/www"
  },
  "devDependencies": {
    "light-server": "^2.5.1",
    "typescript": "^2.9.1",
    "webpack": "^4.12.0",
    "webpack-cli": "^3.0.3"
  }
}
